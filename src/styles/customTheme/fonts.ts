import { type DeepPartial, type Theme } from "@chakra-ui/react";

const fonts: DeepPartial<Theme["fonts"]> = {
  body: "'IBM Plex Sans JP', sans-serif",
  heading: "'IBM Plex Sans JP', sans-serif",
};

export default fonts;
